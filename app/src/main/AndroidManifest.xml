<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drawable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BitmapDrawableActivity"
            android:label="@string/bitmap"/>
        <activity android:name=".LayerDrawableActivity"
            android:label="@string/layer"/>
        <activity android:name=".StateListDrawableActivity"
            android:label="@string/state_list"/>
        <activity android:name=".LevelListDrawableActivity"
            android:label="@string/level_list"/>
        <activity android:name=".TransitionDrawableActivity"
            android:label="@string/transition"/>
        <activity android:name=".InsertDrawableActivity"
            android:label="@string/insert"/>
        <activity android:name=".ClipDrawableActivity"
            android:label="@string/clip"/>
        <activity android:name=".CustomDrawableActivity"
            android:label="@string/custom"/>
    </application>

</manifest>
